#pragma once

#include <complex>
#include <numbers>

/// Type alias for double precision complex numbers
using Complex = std::complex<double>;

/// π — C++20: std::numbers::pi
inline constexpr double PI = std::numbers::pi;
